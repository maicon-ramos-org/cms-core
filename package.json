{
  "name": "cms-core",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm cobertura && node scripts/confere-core-sem-marca.mjs && node --test scripts/confere-core-sem-marca.test.mjs && pnpm -r typecheck && pnpm -r test",
    "cobertura": "node scripts/confere-cobertura.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "esbuild",
      "unrs-resolver"
    ]
  }
}
